>>> import serial
>>> ser = serial.Serial('/dev/ttyACM0')

ser.inWaiting() ## ...

## Arduino: 
# Serial.print('A');
>>> x = ser.read(1).decode(); x ### 'A'
>>> ser.flushInput()

## Arduino: 
# Serial.write(firstSensor); 
# Serial.write(secondSensor);
# Serial.write(thirdSensor);
>>> ser.write(b'y'); resp = ser.read(3)
>>> ser.inWaiting() ## 0

>>> resp; type(resp); type(resp[0])
>>> ord(chr(resp[0]))


